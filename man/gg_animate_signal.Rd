% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_animate_signal.R
\name{gg_animate_signal}
\alias{gg_animate_signal}
\title{Animate Signal Data with Behavior Codes Using ggplot2 and gganimate}
\usage{
gg_animate_signal(
  rec_dt,
  output_file,
  duration = 30,
  fps = 10,
  time_unit = "hours",
  alpha = 0.8,
  plot_title = "EPG Signal Animation",
  figure_type = "wide",
  bin_width = NULL
)
}
\arguments{
\item{rec_dt}{A \code{data.table} with columns \code{time}, \code{signal}, and \code{behavior}.}

\item{output_file}{The file path where the GIF will be saved (e.g., \code{"path/to/output.gif"}).}

\item{duration}{Duration of the animation in seconds. Default is \code{30}.}

\item{fps}{Frames per second for the animation. Default is \code{10}.}

\item{time_unit}{The time unit for the x-axis: \code{"hours"} or \code{"seconds"}. Default is \code{"hours"}.}

\item{alpha}{The transparency level for the lines (0 = fully transparent, 1 = fully opaque). Default is \code{0.8}.}

\item{plot_title}{The title of the plot. Default is \code{"EPG Signal Animation"}.}

\item{figure_type}{The aspect ratio of the plot: \code{"wide"} for rectangular or \code{"square"} for square. Default is \code{"wide"}.}

\item{bin_width}{The width of time bins for aggregating data (in seconds). Default is \code{NULL} (no binning).}
}
\value{
An animated \code{ggplot} object saved as a GIF file.
}
\description{
Creates an animated plot of the signal data using ggplot2 and gganimate, color-coded by behavior.
The animation shows the signal over time, with an option to bin the data.
By default, all data points are used without binning.
}
\examples{
\dontrun{
# Animate and save the GIF without binning (default)
gg_animate_signal(
  rec_dt,
  output_file = "C:/Users/danie/Desktop/epg_signal.gif"
)

# Animate with binning (e.g., bin width of 60 seconds)
gg_animate_signal(
  rec_dt,
  output_file = "C:/Users/danie/Desktop/epg_signal_binned.gif",
  bin_width = 60
)
}
}
