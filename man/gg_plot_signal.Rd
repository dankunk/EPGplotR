% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_plot_signal.R
\name{gg_plot_signal}
\alias{gg_plot_signal}
\title{Plot Signal Data with or without Behavior Codes Using ggplot2}
\usage{
gg_plot_signal(
  rec_dt,
  downsample_rate = 10,
  time_unit = NULL,
  alpha = 0.5,
  plot_title = "EPG Signal",
  linewidth = 0.5,
  output_file = NULL,
  figure_type = "wide",
  range = NULL,
  range_unit = NULL,
  no_ana = FALSE
)
}
\arguments{
\item{rec_dt}{A \code{data.table} with columns \code{time} and \code{signal}. If behavior codes are available,
it should also include a \code{behavior} column.}

\item{downsample_rate}{The rate to downsample the data (e.g., \code{10} for every 10th point). Default is \code{10}.}

\item{time_unit}{The time unit for the x-axis: \code{"hours"}, \code{"seconds"}, or \code{NULL} for automatic selection.
Default is \code{NULL}.}

\item{alpha}{The transparency level for the lines (0 = fully transparent, 1 = fully opaque). Default is \code{0.5}.}

\item{plot_title}{The title of the plot. Default is \code{"EPG Signal"}.}

\item{linewidth}{The line width for the ggplot2 line plot. Default is \code{0.5}.}

\item{output_file}{The file path where the plot will be saved (e.g., \code{"path/to/plot.png"}).
If \code{NULL}, the plot is displayed in the R plotting window. Default is \code{NULL}.}

\item{figure_type}{The aspect ratio of the plot: \code{"wide"} for rectangular or \code{"square"} for square.
Default is \code{"wide"}.}

\item{range}{A numeric vector of length 2 specifying the start and end times for plotting.
If \code{NULL}, the entire time range is plotted. Default is \code{NULL}.}

\item{range_unit}{The time unit for the \code{range} parameter: \code{"hours"} or \code{"seconds"}.
Must be specified if \code{range} is provided. Default is \code{NULL}.}

\item{no_ana}{Logical value indicating whether to plot without behavior annotations.
If \code{TRUE}, behavior annotations are ignored and the plot shows only the raw signal.
Default is \code{FALSE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a static plot of the signal data using ggplot2. If behavior codes are available,
the plot can be color-coded by behavior. When a specific range is provided, the function
automatically adjusts the time unit (seconds or hours) based on the range. The x-axis labels
are formatted to one decimal place. You can adjust the line transparency, specify a custom plot
title, save the plot to disk, choose the line width, specify a time range to plot, and
choose between a wide or square figure.
}
\examples{
\dontrun{
# Plot without behavior annotations, automatic time unit
p <- gg_plot_signal(rec_dt, no_ana = TRUE)

# Plot with behavior annotations (default behavior), automatic time unit
p <- gg_plot_signal(rec_dt)

# Plot with specified range, automatic time unit
p <- gg_plot_signal(rec_dt, range = c(0, 526), range_unit = "seconds", no_ana = TRUE)
}
}
